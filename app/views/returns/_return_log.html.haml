
.row
  .col-xs-12
    %table.table
      %tr
        %th RA #
        %th Actions
      - @returns.each do |record|
        %tr
          %td= record.id
          %td= link_to "view", edit_return_path(record)
